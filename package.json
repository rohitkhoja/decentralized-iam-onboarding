{
  "name": "decentralized-iam-onboarding",
  "version": "1.0.0",
  "description": "Decentralized IAM system for employee onboarding using Verifiable Credentials and DIDs",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "node": "hardhat node",
    "clean": "hardhat clean",
    "coverage": "hardhat coverage"
  },
  "keywords": [
    "IAM",
    "DID",
    "Verifiable Credentials",
    "Blockchain",
    "Identity",
    "Access Management"
  ],
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.6.1",
    "ethers": "^6.8.0"
  }
}
